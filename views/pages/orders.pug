html
    head
        style
            include ../../style.css
    body
        include ../partials/navbar

        h1 Order #{order.order_id}
        hr

        p Status: #{order.status}
        p Date of Order: #{order.date_of_order}
        p Destination: #{order.destination}

        hr

        h2 Content:
        p
        each book, index in books
          div.bookbrowse
            | Title: 
            a(href="/books/" + book.isbn) #{book.name}
            |
            p Price: $#{book.price}

        hr

        p Total: #{total}

        



            


